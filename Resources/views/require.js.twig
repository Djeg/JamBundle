{% if require %}

{{require|raw}}

jam['baseUrl'] = "{{baseUrl|raw}}";

jam['paths'] = {};

{% for name, path in bundles %}
	
jam['paths']['{{name|raw}}'] = "{{baseUrl|raw}}/{{path|raw}}";

{% endfor %}

require.config(jam);

{% endif %}

// bundles bootstrap :
{% for bootstrap in bootstraps %}

{{bootstrap|raw}}

{% endfor %}