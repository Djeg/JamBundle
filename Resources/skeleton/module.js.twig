/**
 * {{module}}
 * cool description stuff here ....
 */
define([], function()
{
	/**
	 * @var Object {{canonicalModule}}, the public module
	 * stuff
	 * @access private
	 */
	var {{canonicalModule}} = {};

	return {{canonicalModule}};
});